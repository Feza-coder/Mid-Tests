public class TaxiDriver extends OutsideJob {

    public TaxiDriver(double salary, String startShift, String endShift) {
        super(salary, startShift, endShift, "Drives customers to destinations.");
    }
}
